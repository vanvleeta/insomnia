# data/ is deliberately NOT ignored. It holds the indexes for local sources,
# which is exactly what the deployed site serves, and the sync and push
# workflows commit into it. Ignoring it makes those workflows stage nothing,
# report "unchanged", and exit successfully while no data is ever deployed.

node_modules
.DS_Store
